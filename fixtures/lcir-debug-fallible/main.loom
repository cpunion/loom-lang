module lcir_debug_fallible

fn increment(value Int) Int {
    value + 1
}

fn identity(value Int) Int {
    value
}

pub fn main() Unit {
    let answer = identity(increment(41))
    if answer == 42 {
        Unit
    } else {
        discard 1 / 0
        Unit
    }
}
