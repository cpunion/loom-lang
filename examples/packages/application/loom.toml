schema = 2
language = "0.3"

[module]
name = "application"
version = "0.1.0"

[dependencies]
utility = { path = "../utility", version = "^1" }

[[target]]
name = "app"
kind = "bin"
entry = "application.start"
