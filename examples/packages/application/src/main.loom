module application

import utility.math.increment

pub fn start() {
    let answer = increment(41)
    assert answer == 42
}

test fn dependencyWorks() {
    let answer = increment(1)
    assert answer == 2
}
