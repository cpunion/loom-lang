module foundation.arithmetic

import foundation.money.Money

pub fn add_money(left Money, right Money) Result[Money, ConstraintError] {
    Money(left + right)
}

pub fn difference(left Money, right Money) Float {
    left - right
}
